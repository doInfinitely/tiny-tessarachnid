# Recovery Notes — session 0a90eaa8 (2026-03-20..03-23)

Source: `/home/remy/recovered_sessions/misty-dancing-wind/`

The session ran in `/home/remy/tiny-tessarachnid` but the working tree was an
**uncommitted refactor** that no longer exists locally — the last commit on `main`
(f8ea578, 2026-03-02) predates the refactor by 2.5 weeks. Recovery is partial.

## Files NOT in the repo that the session referenced

| File | Lines | Coverage | Notes |
|---|---|---|---|
| `train_02_char.py` | 1270 | **57% verbatim + stitched** | Hierarchical char-only training (BlockBalanced, CharBalanced, SlidingWindow datasets, joint loss). See `recovered/train_02_char.py.partial` (raw 57%) **and `recovered/train_02_char.py.stitched`** (1302-line py-compileable stitch — 723 recovered lines + 7 reconstructed gap-fills + all 6 session edits applied, generated by `stitch_train_02_char.py`). |
| `detect_characters.py` | 239 | **38%** | Detector/sliding-window logic. Most of it lives in `../glyph-faerie/glyph_faerie/detection/detector.py` per glyph-faerie CLAUDE.md. See `recovered/detect_characters.py.partial`. |
| `annotate_tesseract.py` | ≥100 | first 100 lines only | Spatial chaining annotator. See `annotate_tesseract.py.recovered_partial` at repo root. Most of it lives in `../glyph-faerie/glyph_faerie/detection/chaining.py` per glyph-faerie CLAUDE.md. |

## Other refactor traces (read but not edited)

The session read **older or differently-refactored versions** of files that DO
exist in the repo. These don't need recovery, but illustrate the refactor's shape:

- `create_lists.py` (session: 80 lines w/ docstring + legacy section; repo: 157 lines with full Unicode block detail) — repo version is more complete.
- `train_03.py`, `train_04.py` (session imports `from augmentation import AugmentedDataset, default_pipeline`; repo has no `augmentation` module) — the refactor was extracting an augmentation pipeline that never landed.

## Stitched train_02_char.py — what's reconstructed vs verbatim

The `train_02_char.py.stitched` file is assembled by `stitch_train_02_char.py`.
Verbatim recovered code is unmarked; **all reconstructed regions are wrapped in
`# [RECONSTRUCTED ...]` / `# [/RECONSTRUCTED]` markers** so you can audit them.

Reconstructed regions and their basis:

| Region | Lines (in stitched) | Source for the reconstruction |
|---|---|---|
| Header (docstring, imports, constants) | 1-58 | Patterns from `train_03.py` / `train_04.py`; `CHAR_INPUT_SIZE=128` from `glyph-faerie/.../model.py` |
| `HierarchicalCharNet.predict()` body + `_render_char` + `_render_char_in_context` | 233-340 | `predict()` mirrors `glyph-faerie/.../model.py:HierarchicalCharNet.predict()` (which was *ported* from this file). Render helpers reconstructed from intent (PIL ImageFont, centered scale-to-fit, sibling layout). |
| `_pool_init` / `_render_task` / `_parallel_render` signature changes | 354-396 | Session Edit 1's new `_parallel_render(tasks, _block_char_lists, "...")` call requires this. Worker-global sibling map pattern. |
| `BlockBalancedDataset` char-binning + tensor allocation | 404-422 | Mirrors the equivalent block in `CharBalancedDataset` from the recovered partial |
| `BlockBalancedDataset` trim/shuffle tail + Stage B class header | 476-499 | Symmetric to the `CharBalancedDataset` tail visible in partial |
| `HierarchicalLoss.forward` | 735-762 | Standard hierarchical loss; signature confirmed by `_DPTrainWrapper.forward` which is verbatim recovered |
| Stage A training-loop tail + Stage B setup + `_run_epoch` header | 865-930 | `_run_epoch` body 980-1052 is verbatim recovered; this stitches Stage A/B to it |
| `train()` body: workers, char-list load, model init, ecological table head | 1023-1097 | Bash logs confirm "Unicode chars: 61876, active blocks: 218" output; backbone-transfer log line copied; ecological dict patterned after the recovered tail at line 1161+ |
| Tail: Stage C training loop, argparse, `__main__` | 1220-1283 | Argparse derived from session bash invocations (`--batch-size 512 --ecological --resume-stage-c --joint-spc 10000 --epochs-joint 50`); Stage C loop mirrors Stage A/B loops |

### Dependencies the stitched file requires — STATUS: back-ported

The session was using a refactored `create_lists.py` / `generate_training_data.py`
that exported symbols absent in the current repo HEAD. These have been back-ported:

- ✅ **`create_lists.py`** — appended `_build_block_table`, `_UNICODE_BLOCKS`,
  `BLOCK_NAMES`, `BLOCK_NAME_TO_IDX`, `NUM_BLOCKS`, `get_unicode_block`,
  `get_block_index`, `build_block_char_map` (verbatim copy from
  `../glyph-faerie/glyph_faerie/detection/blocks.py`). Also added
  `from __future__ import annotations` at top so the `list[str]` type hints
  parse on Python 3.8+. Verified `NUM_BLOCKS=297` matches the value the session
  checkpoint stored (`num_blocks: 297`). Original `create_font_list()` and
  `create_character_list()` left untouched.
- ✅ **`generate_training_data.py`** — added `get_unicode_chars(verbose=True)`.
  Returns `(chars, char_to_fonts)` where `chars` is a sorted list and
  `char_to_fonts[ch]` is a list of `(font_path, face_idx)` tuples.
  Uses `fontTools.ttLib.TTFont` / `TTCollection` to read cmap tables directly
  (no rendering), then NFKC-normalizes to collapse canonical variants. This
  function is *reconstructed* — not present in the session recording — but
  matches the bash log signature: `Loading Unicode character list... NFKC
  normalization: merged 2895 variant chars → 61876 unique chars remaining`.
  Requires the `fonttools` PyPI package at run-time.

Verified by AST scan: every `from create_lists import …` and
`from generate_training_data import …` in the stitched file resolves to a
defined name in the back-ported files.

### `detect_characters.py` — STATUS: back-ported

- ✅ **`detect_characters.py`** (801 lines, new file) — reverse-ported from
  `../glyph-faerie/glyph_faerie/detection/detector.py`. Strategy:
  - Lines 1-80 are verbatim from the 38%-recovered partial: tiny-tessarachnid-
    specific docstring (with Usage + CLI examples), full imports list including
    argparse/hashlib/ImageDraw/ImageFont, `MEGA_BLOCK_THRESHOLD = 256`, Hangul
    decompose/compose helpers, EmbeddingIndex class header.
  - Lines 81-135: rest of `EmbeddingIndex` (save/load/lookup_batch), copied
    verbatim from glyph-faerie.
  - Lines 136-159: `DetectorConfig` — uses the partial's inline-commented
    field declarations (lines 230-239 of original), then extends with the
    remaining fields from glyph-faerie (confidence_threshold, nms_iou_threshold,
    max_batch_size, min_pixel_std, min_edge_density).
  - Lines 160-750: `CharacterDetector`, `load_detector`, `_extract_windows`,
    `_reject_blank_windows`, `detect_characters`, `detect_characters_raw`,
    `apply_block_priors`, `apply_nms` — copied from glyph-faerie with
    imports patched: `glyph_faerie.detection.model` → `train_02_char`,
    `glyph_faerie.detection.blocks` → `create_lists`.
  - Lines 752-800: reconstructed CLI entry (`_cli_main` + `if __name__`) matching
    the partial's `.venv/bin/python detect_characters.py image.png --model X --visualize`
    usage example.
- Reverse-port script saved as `recovered/backport_detect_chars.py`.
- AST scan confirms all three lazy-imported symbols (`_extract_windows`,
  `DetectorConfig`, `_reject_blank_windows`) resolve cleanly from the
  stitched `train_02_char.py`'s `SlidingWindowDataset.__init__`.

### Quick caveats on the back-ported detect_characters.py

- `apply_block_priors` and `detect_characters_raw` are new additions from
  glyph-faerie that the *original* tiny-tessarachnid version may not have had
  (we only saw lines 1-80 and 230-239 of the original). If a downstream
  caller specifically expects the original's behavior, double-check these.
- `load_detector` still expects a `block_to_chars` key in the checkpoint dict.
  The session's checkpoint had that key (`'block_to_chars'` in keys list per
  session bash output), so this should work with the existing `model_02_char.pth`.

## Session-confirmed edits applied to the stitched file

All six Edit operations captured by the session are applied in their final form:

1. **2026-03-20 17:27** — BlockBalanced render: pass `block_idx` (not the chars list) to `_parallel_render` to enable in-context rendering via sibling lookup.
2. **2026-03-20 18:22** — Replace `CharBalancedDataset` from pre-rendered tensor allocation to lazy on-demand rendering in `__getitem__`. Saves GBs of upfront memory.
3. **2026-03-20 18:22** — Bump `char_nw = max(nw, 8)` for char dataset (rendering happens in workers).
4. **2026-03-22 23:38** — Switch joint-stage dataset from `BlockBalancedDataset` to `CharBalancedDataset` (lazy renders fresh images every epoch → can't overfit).
5. **2026-03-23 06:09** — Add new `SlidingWindowDataset` class: synthesizes pages with `SyntheticPage`, runs the real `_extract_windows` / `_reject_blank_windows` from `detect_characters`, matches windows to GT chars by IoU. "Zero domain gap" training data.
6. **2026-03-23 06:09** — Add `--sliding-window` / `--sw-pages` CLI option in joint stage to switch between `CharBalancedDataset` and `SlidingWindowDataset`.

Full OLD/NEW strings are in `/tmp/recovered_reads/` (the source `f*.txt` JSONL).

## Last user messages in session (for context)

- "can we re use the best checkpoint and fix the issues" (2026-03-22 16:59)
- "more epochs?" (2026-03-22 19:03)
- "how can it overfit if we're using synth data?" (2026-03-22 23:38) — motivated the lazy/SlidingWindow rewrite
- "which checkpoint was the best?" / "what's the latin block accuracy?" / "lets test first" / "lets make them the same" (2026-03-23 06:02..06:08)
